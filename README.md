## Get Started

1. Install docker and docker compose then :

    ```bash
    docker compose build
    ```
    ```bash
    docker compose up
    ```

2. Open sagger:
    ```bash
        http://localhost:8800/docs
    ```
